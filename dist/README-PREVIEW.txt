This folder holds ONE thing: the build for your GitHub Pages preview URL.

  preview-github-pages/   -> norvixhq.github.io/norvixco.com/

The normal production build lives in ../site/ , which is where
`node build.js` writes. Use that one for the real domain.
